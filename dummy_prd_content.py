DUMMY_PRD_TEXT = {
  "Executive Summary": "This PRD defines an implementation-ready specification for the **z/OS Connect API Requester Claims Sample**, covering CICS online claim processing and IMS message-driven claim evaluation with external rules adjudication via z/OS Connect EE [CONFIRMED].\\n\\nA single canonical outcome model is established to remove prior semantic conflicts:\\n- `APPROVED`\\n- `REJECTED`\\n- `PENDING_REVIEW`\\n- `ERROR` [INFERRED]\\n\\nChannel-specific display mappings are retained for backward compatibility:\\n- CICS (`RSP-CLAIM-STATUS` X(4)): `OKAY`->APPROVED, `RJCT`->REJECTED, `PEND`->PENDING_REVIEW, `ERRO`->ERROR [INFERRED]\\n- IMS (`OUT-CLAIM-STATUS` X(10)): `ACCEPTED`->APPROVED, `REJECTED`->REJECTED, `PENDING`->PENDING_REVIEW, `ERROR`->ERROR [INFERRED]\\n\\nThe architecture and core runtime behavior are validated from source:\\n- COBOL programs call `BAQCSTUB` and do not implement native HTTP/JSON [CONFIRMED]\\n- CICS program `CLAIMCI0` supports action routing `S/R/U` with VSAM `CLAIMCIF` persistence [CONFIRMED]\\n- IMS program `IMSCLAIM` processes input messages using DL/I (`GU/GN/ISRT`) and terminates on PCB status `QC` [CONFIRMED]\\n- IMS performs `BAQCTERM` cleanup after API invocation [CONFIRMED]\\n\\nThis PRD includes testable functional requirements, quantified NFRs, explicit data constraints, failure-path requirements, and a risk register suitable for implementation and validation [INFERRED].",
  "System Overview": "## Purpose\nEnable legacy COBOL applications to consume an external health insurance claim rules API through z/OS Connect API Requester, preserving core COBOL business logic while externalizing REST/JSON integration concerns [CONFIRMED].\n\n## Scope\n### In Scope\n- CICS transaction processing in `CLAIMCI0`: Submit, Read, Update against VSAM KSDS `CLAIMCIF` [CONFIRMED]\n- IMS message-driven processing in `IMSCLAIM` via IO-PCB message queue [CONFIRMED]\n- API mediation through `BAQCSTUB` and API metadata from `CLAIMINF` (`Sample-Node.js-Claims-Rule-API_1.0`, `%2Fclaim%2Frule`, `GET`) [CONFIRMED]\n- Operational logging via CICS CSMT queue and IMS DISPLAY logs [CONFIRMED]\n\n### Out of Scope\n- Authoritative enterprise IAM configuration internals in z/OS Connect server runtime [UNKNOWN]\n- External API decision algorithm internals beyond returned status/reason payload [UNKNOWN]\n\n## Users and Personas\n- **CICS Operator/User**: invokes CICS transaction and receives claim response payload [INFERRED]\n- **IMS Integration Consumer**: submits IMS input messages and consumes output queue responses [CONFIRMED]\n- **Mainframe Operations**: monitors CSMT and IMS logs, handles failures/retries [INFERRED]\n- **Platform Engineer**: deploys JCL, VSAM, CICS resources, and z/OS Connect artifacts [DOC-ONLY]\n\n## Assumptions and Dependencies\n- z/OS Connect EE installed and reachable from CICS/IMS runtime [DOC-ONLY]\n- `BAQCSTUB` and `BAQCTERM` load modules available [CONFIRMED]\n- VSAM KSDS `CLAIMCIF` pre-created and CICS FILE resource defined [DOC-ONLY]\n- External claim rules API endpoint available and stable [DOC-ONLY]\n- CICS channel/container invocation used for `CLAIMCI0` [CONFIRMED]",
  "Functional Requirements": "## Feature Area A: CICS Claim Processing\n\n### FR-001 Action Routing\n**Requirement:** The system shall route CICS requests by `REQ-CLAIM-ACTION` value `S`, `R`, or `U` [CONFIRMED].\n- **Input:** `CLAIMRQC` request container with `REQ-CLAIM-ACTION` [CONFIRMED]\n- **Processing:** `DO-MAIN-CONTROL` dispatches to submit/read/update; invalid action sets failure response [CONFIRMED]\n- **Output:** `CLAIMRSC` response container with status and message [CONFIRMED]\n- **Acceptance criteria:**\n  - Given action=`S`, when request is processed, then submit path executes and CICS response container is returned.\n  - Given action=`R`, when request is processed, then read path executes and response contains found/not-found message.\n  - Given action=`U`, when request is processed, then update path executes and rewrite is attempted.\n  - Given action not in `{S,R,U}`, then no VSAM write/rewrite occurs and output message indicates invalid action.\n\n### FR-002 Submit Claim - API Invocation\n**Requirement:** The system shall call the external claim rules API through `BAQCSTUB` for submit requests [CONFIRMED].\n- **Input:** `REQ-CLAIM-TYPE`, `REQ-CLAIM-AMOUNT` [CONFIRMED]\n- **Processing:** Build `CLAIMREQ` (`claimType-length`, `claimType`, `claimAmount`) and invoke `BAQCSTUB` with `API-INFO` [CONFIRMED]\n- **Output:** Populated `CLAIMRSP` structure used for status mapping [CONFIRMED]\n- **Acceptance criteria:**\n  - Given valid submit input, when API call succeeds, then `BAQCSTUB` returns and response fields are available for mapping.\n  - Given API/stub failure, then submit returns deterministic failure status mapping (`ERRO`/`PEND` per BR mapping) and output message.\n\n### FR-003 Submit Claim - Status Mapping and Persistence\n**Requirement:** The system shall map API decision to canonical status and persist a claim record to `CLAIMCIF` [INFERRED].\n- **Input:** API response status text (`Accepted` or other), request claim payload [CONFIRMED]\n- **Processing:** Apply canonical mapping rules, set CICS display status, write VSAM by `REQ-CLAIM-ID` [CONFIRMED]\n- **Output:** VSAM record created and response container returned [CONFIRMED]\n- **Acceptance criteria:**\n  - Given API status `Accepted`, then canonical status=`APPROVED`, CICS status=`OKAY`, and VSAM WRITE success.\n  - Given API status non-`Accepted`, then canonical status=`PENDING_REVIEW` (default policy), CICS status=`PEND`, and VSAM WRITE success.\n  - Given duplicate key on WRITE, then no overwrite occurs, response indicates duplicate claim ID, and status=`ERRO`.\n  - Given VSAM I/O failure, then response indicates persistence failure and status=`ERRO`.\n\n### FR-004 Read Claim\n**Requirement:** The system shall read an existing claim by key from `CLAIMCIF` [CONFIRMED].\n- **Input:** `REQ-CLAIM-ID`, action `R` [CONFIRMED]\n- **Processing:** `CICS READ FILE CLAIMCIF` by key [CONFIRMED]\n- **Output:** Found record mapped to `CLAIMRSC` or not-found message [CONFIRMED]\n- **Acceptance criteria:**\n  - Given existing key, then response includes claim fields and success message.\n  - Given missing key, then response includes not-found message and no abend.\n\n### FR-005 Update Claim Status\n**Requirement:** The system shall update claim status using `READ UPDATE` followed by `REWRITE` [CONFIRMED].\n- **Input:** `REQ-CLAIM-ID`, `REQ-FILLER` (requested status), action `U` [CONFIRMED]\n- **Processing:** Validate requested status, read record for update, apply status, rewrite record [INFERRED]\n- **Output:** Updated record state and response message [CONFIRMED]\n- **Acceptance criteria:**\n  - Given existing key and valid status, then rewrite succeeds and response confirms update.\n  - Given missing key, then rewrite is not attempted and response indicates not found.\n  - Given invalid status in `REQ-FILLER`, then update rejected with validation message.\n\n## Feature Area B: IMS Message-Driven Processing\n\n### FR-006 IMS Loop and Message Handling\n**Requirement:** The system shall process IMS input messages and emit one output message per input until PCB status `QC` [CONFIRMED].\n- **Input:** `INPUT-MSG` (`IMSCLAIC`) via `CBLTDLI GU/GN` [CONFIRMED]\n- **Processing:** For each message: build API request, invoke API, map decision, issue `ISRT` [CONFIRMED]\n- **Output:** `OUTPUT-MSG` with type/status/amount/message [CONFIRMED]\n- **Acceptance criteria:**\n  - Given N valid input messages before `QC`, then N output messages are inserted.\n  - Given `QC`, processing stops cleanly.\n\n### FR-007 IMS API Cleanup\n**Requirement:** The system shall invoke `BAQCTERM` after each IMS API call [CONFIRMED].\n- **Input:** Completed `BAQCSTUB` call context [CONFIRMED]\n- **Processing:** Execute `CALL 'BAQCTERM'` in `CALL-API` path [CONFIRMED]\n- **Output:** Stub session/cache cleanup [INFERRED]\n- **Acceptance criteria:**\n  - For each processed input message with attempted API call, `BAQCTERM` is invoked once.\n\n## Feature Area C: Error Handling\n\n### FR-008 API/Stub Failure Handling\n**Requirement:** The system shall return deterministic error statuses/messages when `BAQCSTUB` fails [CONFIRMED].\n- **Input:** Non-success API/stub outcome [CONFIRMED]\n- **Processing:** Map failure to channel-specific error status and diagnostic message [CONFIRMED]\n- **Output:** CICS response or IMS output marked error path [CONFIRMED]\n- **Acceptance criteria:**\n  - CICS: failure yields `PEND` or `ERRO` per BR policy with explanatory message.\n  - IMS: failure yields `ERROR` and output message is populated.\n\n### FR-009 VSAM Failure Handling\n**Requirement:** The system shall handle VSAM duplicate-key and I/O errors without data corruption [INFERRED].\n- **Input:** WRITE/READ/REWRITE failure conditions [INFERRED]\n- **Processing:** Detect RESP/RESP2, stop unsafe write path, return explicit error text [INFERRED]\n- **Output:** No partial updates; deterministic failure response [INFERRED]\n- **Acceptance criteria:**\n  - Duplicate key write does not overwrite existing record.\n  - REWRITE failure preserves pre-update record.",
  "Data Model": "## Canonical Type System\nTarget logical representation for validation and testing:\n- `string(n)` fixed-length char-compatible\n- `decimal(p,s)` fixed decimal\n- `enum` constrained vocabulary [INFERRED]\n\n## Entity: ClaimRequestCICS (`CLAIMRQC`) [CONFIRMED]\n- `REQ-CLAIM-ID` : `string(8)`; **PK candidate**; pattern `^[A-Z0-9]{8}$` [INFERRED]\n- `REQ-CLAIM-TYPE` : `string(8)`; enum `{DRUG,DENTAL,MEDICAL,OTHER}` [INFERRED]\n- `REQ-CLAIM-AMOUNT` : COBOL `COMP-2`; logical `decimal(9,2)`; range `0.01..9999999.99` [INFERRED]\n- `REQ-CLAIM-DATE` : `string(10)`; format `YYYY-MM-DD` [INFERRED]\n- `REQ-CLAIM-DESC` : `string(21)` [CONFIRMED]\n- `REQ-CLAIM-PROVIDER` : `string(21)` [CONFIRMED]\n- `REQ-FILLER` : `string(4)`; update status input (legacy) [CONFIRMED]\n- `REQ-CLAIM-ACTION` : `enum{S,R,U}` [CONFIRMED]\n\n## Entity: ClaimResponseCICS (`CLAIMRSC`) [CONFIRMED]\n- Core claim fields mirrored from request/record [CONFIRMED]\n- `RSP-CLAIM-STATUS` : `string(4)`; enum `{OKAY,RJCT,PEND,ERRO}` [INFERRED]\n- `RSP-CLAIM-OUTPUT-MESSAGE` : `string(80)` [CONFIRMED]\n- `RSP-CLAIM-CICS-RESP` : integer [CONFIRMED]\n- `RSP-CLAIM-CICS-RESP2` : integer [CONFIRMED]\n\n## Entity: RuleApiRequest (`CLAIMREQ`) [CONFIRMED]\n- `claimType-length` : int16/comp-5 [CONFIRMED]\n- `claimType` : `string(255)` [CONFIRMED]\n- `claimAmount` : numeric `COMP-2` [CONFIRMED]\n\n## Entity: RuleApiResponse (`CLAIMRSP`) [CONFIRMED]\n- `Xstatus2` : `string(255)` decision text used by both channels [CONFIRMED]\n- `reason2` : `string(255)` rationale [CONFIRMED]\n- Additional variable-length fields (`claim-type2`, `amount2`) [CONFIRMED]\n\n## Entity: ApiInfo (`CLAIMINF`) [CONFIRMED]\n- `BAQ-APINAME='Sample-Node.js-Claims-Rule-API_1.0'`\n- `BAQ-APIPATH='%2Fclaim%2Frule'`\n- `BAQ-APIMETHOD='GET'`\n- associated length fields present [CONFIRMED]\n\n## Entity: ClaimRecordStore (`CLAIMCIF` KSDS)\n- Logical key: claim ID (8 chars) [CONFIRMED]\n- Operations used: READ, READ UPDATE, WRITE, REWRITE [CONFIRMED]\n- Record length/offset/AIX details: not present in analyzed source [UNKNOWN]\n- Integrity rule: key uniqueness required for submit [INFERRED]\n\n## Entity: IMS Message Contract (`IMSCLAIC`) [CONFIRMED]\n- Input: `IN-TRANCODE`, `IN-CLAIM-TYPE`, `IN-CLAIM-DATE`, `IN-CLAIM-AMOUNT`, `IN-CLAIM-DESC` [CONFIRMED]\n- Output: `OUT-CLAIM-TYPE`, `OUT-CLAIM-STATUS`, `OUT-CLAIM-DESC`, `OUT-CLAIM-AMOUNT`, `OUT-MESSAGE` [CONFIRMED]\n- PCB linkage includes status code and terminal metadata [CONFIRMED]\n\n## Relationships\n- `ClaimRequestCICS.REQ-CLAIM-ID` -> `ClaimRecordStore.key` (1:1 for active record) [INFERRED]\n- `RuleApiRequest`/`RuleApiResponse` are transient integration payloads for both `CLAIMCI0` and `IMSCLAIM` [CONFIRMED]",
  "Process Flows": "## Flow 1: CICS Submit (Happy Path)\n1. Receive request via CICS container (`GET CONTAINER`) [CONFIRMED]\n2. Validate action=`S` and input constraints [INFERRED]\n3. Build API request (`CLAIMREQ`) and call `BAQCSTUB` [CONFIRMED]\n4. Map API response to canonical status then CICS display status [INFERRED]\n5. Write record to `CLAIMCIF` [CONFIRMED]\n6. Populate `CLAIMRSC`, put response container, return [CONFIRMED]\n\n## Flow 2: CICS Read\n1. Receive action=`R` request [CONFIRMED]\n2. `READ FILE CLAIMCIF` by `REQ-CLAIM-ID` [CONFIRMED]\n3. Return found record or not-found response [CONFIRMED]\n\n## Flow 3: CICS Update\n1. Receive action=`U` request [CONFIRMED]\n2. Validate requested status from `REQ-FILLER` [INFERRED]\n3. `READ FILE ... UPDATE` [CONFIRMED]\n4. Apply new status and `REWRITE FILE` [CONFIRMED]\n5. Return update result [CONFIRMED]\n\n## Flow 4: IMS Processing Loop\n1. `GU` first input message [CONFIRMED]\n2. While PCB status != `QC`: [CONFIRMED]\n   - Build request and call `BAQCSTUB` [CONFIRMED]\n   - Map response to IMS output status [CONFIRMED]\n   - `ISRT` output message [CONFIRMED]\n   - Call `BAQCTERM` [CONFIRMED]\n   - `GN` next message [CONFIRMED]\n3. Exit on `QC` [CONFIRMED]\n\n## Failure Flows\n### Flow ERR-A: API/Stub Failure (CICS)\n- On `BAQCSTUB` failure: set error/pending status, set output message, write diagnostics to CSMT, return response [CONFIRMED]\n\n### Flow ERR-B: VSAM Duplicate Key on Submit\n- On WRITE duplicate: do not overwrite, return explicit duplicate claim ID message, status `ERRO` [INFERRED]\n\n### Flow ERR-C: IMS API Failure Mid-Loop\n- Current message returns `ERROR`; processing continues to next message until `QC` [INFERRED]\n- Transaction-level rollback/poison queue handling not implemented in source [UNKNOWN]",
  "Business Rules": "## Canonical Decision Policy\nCanonical statuses are `APPROVED`, `REJECTED`, `PENDING_REVIEW`, `ERROR` [INFERRED].\n\n| BR ID | Rule Statement | Type | Verification Method | Source Traceability |\n|---|---|---|---|---|\n| BR-001 | The system shall route CICS actions strictly by `REQ-CLAIM-ACTION` where `S=Submit`, `R=Read`, `U=Update`; any other value is invalid. [CONFIRMED] | Routing | Unit/integration test with action matrix | `CLAIMCI0 DO-MAIN-CONTROL` |\n| BR-002 | The system shall normalize claim type length before API call: `DRUG=4`, `DENTAL=6`, `MEDICAL=7`, default=7. [CONFIRMED] | Transformation | Unit test on type mapping | `CLAIMCI0 DO-CALL-CLAIM-RULE`, `IMSCLAIM CALL-API` |\n| BR-003 | CICS decision mapping shall be: API `Accepted` -> canonical `APPROVED` -> `OKAY`; non-accepted -> canonical `PENDING_REVIEW` -> `PEND`; technical failure -> `ERROR` -> `ERRO` (or `PEND` for legacy compatibility). [INFERRED] | Decision | Contract tests with mocked API responses | `CLAIMCI0 DO-CALL-CLAIM-RULE` |\n| BR-004 | IMS decision mapping shall be: API `Accepted` -> canonical `APPROVED` -> `ACCEPTED`; non-accepted -> canonical `REJECTED` or `PENDING_REVIEW` per configured policy output; technical failure -> `ERROR`. [INFERRED] | Decision | Integration tests across response variants | `IMSCLAIM CALL-API` |\n| BR-005 | Claims in `CLAIMCIF` are uniquely keyed by claim ID; submit with duplicate key is rejected. [INFERRED] | Data integrity | VSAM duplicate-key test | `CLAIMCI0 WRITE FILE CLAIMCIF` |\n| BR-006 | CICS update shall use `READ ... UPDATE` followed by `REWRITE`; direct blind overwrite is prohibited. [CONFIRMED] | Data integrity | Integration test on update path | `CLAIMCI0 DO-UPDATE-CLAIM-REC` |\n| BR-007 | IMS processing continues until IO-PCB status code `QC`. [CONFIRMED] | Loop control | IMS functional loop test | `IMSCLAIM DO-MAIN` |\n| BR-008 | IMS shall invoke `BAQCTERM` after each API call attempt. [CONFIRMED] | Resource lifecycle | Call-count assertion in test harness | `IMSCLAIM CALL-API` |\n| BR-009 | Update status input (`REQ-FILLER`) must be one of allowed status codes; otherwise update is rejected. [INFERRED] | Validation | Negative validation tests | CICS update path policy |\n| BR-010 | Every API/stub failure shall produce deterministic client-visible status and message. [CONFIRMED] | Error handling | Fault-injection tests | `CLAIMCI0`, `IMSCLAIM` error branches |\n\nNote: BR-003/BR-004 formalize previously conflicting channel semantics under one canonical model while preserving channel display constraints [INFERRED].",
  "External Interfaces": "## IF-001 z/OS Connect API Requester Stub\n- **Interface:** COBOL `CALL 'BAQCSTUB'` with request/response pointers, lengths, and `API-INFO` [CONFIRMED]\n- **Used by:** `CLAIMCI0`, `IMSCLAIM` [CONFIRMED]\n- **Success contract:** Response copybook populated for mapping [CONFIRMED]\n- **Failure contract:** Status/message mapping to channel error outputs [CONFIRMED]\n\n## IF-002 External Claim Rules API (via z/OS Connect)\n- **API name:** `Sample-Node.js-Claims-Rule-API_1.0` [CONFIRMED]\n- **Path:** `%2Fclaim%2Frule` [CONFIRMED]\n- **Method:** `GET` [CONFIRMED]\n- **Raw HTTP headers/schema at COBOL boundary:** abstracted by z/OS Connect; not directly present in COBOL code [CONFIRMED]\n- **Decision values from external API beyond `Accepted`:** exact taxonomy not fully specified in source; treated as non-accepted fallback in code [CONFIRMED]\n\n## IF-003 CICS Runtime Interfaces\n- `GET CONTAINER` and `PUT CONTAINER` for request/response payload exchange [CONFIRMED]\n- `READ/WRITE/REWRITE FILE CLAIMCIF` for persistent claim state [CONFIRMED]\n- `WRITEQ TD CSMT` for diagnostics [CONFIRMED]\n- Container names/channel naming conventions are implementation variables in working storage; fixed naming constants not fully documented [UNKNOWN]\n\n## IF-004 IMS Runtime Interfaces\n- `CBLTDLI` with function codes `GU`, `GN`, `ISRT` against IO-PCB [CONFIRMED]\n- IO-PCB includes status code, LTERM, user/date/time fields [CONFIRMED]\n- Message contract defined by `IMSCLAIC` input/output structures [CONFIRMED]\n\n## IF-005 Deployment and Configuration Interfaces\n- Deployment artifacts/process include `.cbl`, `.cpy`, `.jcl`, `.ara`, `.aar`, `.sar`, and z/OS Connect `server.xml` setup as documented [DOC-ONLY]\n- Artifact wiring details and version pinning are not fully derivable from provided source listing [UNKNOWN]",
  "Non Functional Requirements": "## NFR-001 Performance\nThe system shall meet the following targets under normal operating conditions:\n- CICS submit/read/update end-to-end latency: p95 <= 2.0s, p99 <= 3.0s [INFERRED]\n- API mediation segment (`BAQCSTUB` call duration): p95 <= 1.5s [INFERRED]\n- VSAM WRITE/READ/REWRITE service time: p99 <= 50ms [INFERRED]\n\n## NFR-002 Scalability\nThe system shall support:\n- Sustained 100 TPS and burst 250 TPS on CICS claim transactions with no data loss [INFERRED]\n- IMS queue processing throughput >= 50 messages/sec per region instance [INFERRED]\n\n## NFR-003 Availability and Resilience\n- Service availability target: 99.5% during 8x5 business window [INFERRED]\n- On API dependency outage, transactions shall fail deterministically with mapped statuses/messages and no abend [CONFIRMED]\n- Recovery objective targets (RTO/RPO) for VSAM/CICS/IMS are enterprise-defined and currently unspecified in source [UNKNOWN]\n\n## NFR-004 Security\n- API transport shall use z/OS Connect-managed secure connectivity (TLS 1.2+) [DOC-ONLY]\n- Access to VSAM/CICS resources shall be RACF/SAF controlled per enterprise policy [INFERRED]\n- Security control configuration evidence is required at deployment gate; not provable from COBOL source alone [UNKNOWN]\n\n## NFR-005 Observability\n- CICS shall emit operational diagnostics to CSMT for key lifecycle and error events [CONFIRMED]\n- IMS shall emit transaction logs via DISPLAY for start/error/info events [CONFIRMED]\n- Required metrics: `claim_submit_count`, `claim_read_count`, `claim_update_count`, `api_call_duration_ms`, `api_error_count`, `vsam_error_count` [INFERRED]\n\n## NFR-006 Data Integrity and Validation\n- Input validation shall enforce claim ID pattern, action enum, claim amount > 0, and date format before persistence/API invocation [INFERRED]\n- Duplicate claim IDs shall be rejected; no silent overwrite [INFERRED]\n\n## NFR-007 Testability\n- Requirement coverage: 100% of FR and BR mapped to test cases [INFERRED]\n- Automated regression: all error branches in FR-008/FR-009 must have fault-injection tests [INFERRED]\n- Traceability matrix maintained: Requirement -> Code location -> Test case -> Result [INFERRED]",
  "Risks": "## RAID-Style Risk Register\n\n| Risk ID | Risk | Rating | Justification | Mitigation |\n|---|---|---|---|---|\n| R-001 | Decision mapping divergence across channels may reappear over time | HIGH | Existing code currently uses different non-accepted mappings (CICS pending vs IMS rejected) [CONFIRMED] | Enforce canonical status policy in BR-003/BR-004, add contract tests for both channels, require governance sign-off for mapping changes |\n| R-002 | Security posture assumed but not evidenced in code artifacts | HIGH | z/OS Connect security is documented but runtime controls are outside COBOL and unverified here [UNKNOWN] | Add deployment gate requiring TLS/authz config evidence (server.xml/security policy), perform security verification tests |\n| R-003 | VSAM error-path behavior may be inconsistent without explicit RESP/RESP2 standards | MEDIUM | Core VSAM ops confirmed, but detailed duplicate/I/O handling policy was not fully explicit in prior PRD [CONFIRMED] | Implement explicit FR-009 handling matrix and negative tests for DUPKEY/NOTFND/IOERR |\n| R-004 | External API latency/outage impacts transaction SLAs | MEDIUM | Hard dependency on external API via z/OS Connect [CONFIRMED] | Set timeout and retry strategy in requester config, monitor p95 latency, define fallback behavior |\n| R-005 | Data quality issues from insufficient validation (date/amount/status) | MEDIUM | Copybooks define fields but strict validation constraints are not native in source [CONFIRMED] | Implement validation rules (NFR-006, BR-009) and reject malformed input before processing |\n| R-006 | Deployment reproducibility gaps across environments | MEDIUM | Deployment process described in docs but artifact chain and versions not fully traceable in source snapshot [DOC-ONLY] | Publish versioned runbook linking JCL, CICS defs, `.ara/.aar/.sar`, and z/OS Connect config |\n| R-007 | Operational monitoring insufficient for enterprise support | LOW | Logging exists (CSMT/DISPLAY) but metric standards not explicitly implemented [CONFIRMED] | Add standard metric emission and alert thresholds tied to NFR-005 |\n\n## Open Items\n- External API explicit non-`Accepted` status vocabulary requires contract confirmation [UNKNOWN]\n- CLAIMCIF physical schema details (LRECL/key offset/AIX) require platform documentation baseline [UNKNOWN]\n- DR/backup retention policy for claims data requires enterprise policy input [UNKNOWN]"
}

DUMMY_PRD_STREAM_EVENTS = [
  ("🔎 [Step 1/4] Analyzing source files...", 0.3),
  ("Found 272 files in 24 modules (1119209 chars total)", 0.2),
  ("Invoking analysis agent with 24 modules...", 0.3),
  ("[TOOL] list_modules() — 24 modules available", 0.2),
  ("[TOOL] get_module_source('src') — returning 214648 chars", 0.2),
  ("   API call completed", 0.4),
  ("[TOOL] get_module_source('src/packets') — returning 139938 chars", 0.2),
  ("   API call completed", 0.4),
  ("[TOOL] get_module_source('src/encoding') — returning 110180 chars", 0.2),
  ("   API call completed", 0.4),
  ("[TOOL] get_module_source('codegen/generators') — returning 77538 chars", 0.2),
  ("   API call completed", 0.3),
  ("Analysis complete. 24 modules, 374866 chars total", 0.3),
  ("📝 [Step 2/4] Generating PRD from analysis...", 0.3),
  ("Invoking Claude (claude-opus-4-7) for PRD generation...", 2.0),
  ("   Claude (claude-opus-4-7) API call completed", 0.1),
  ("Claude responded in 354s — PRD generated (52615 chars)", 0.3),
  ("🔍 [Step 3/4] Reviewing PRD (iteration 1)...", 0.3),
  ("Invoking Codex (gpt-5.3-codex) for PRD review...", 1.5),
  ("   Codex (gpt-5.3-codex) API call completed", 0.1),
  ("Codex responded in 24s", 0.1),
  ("Review score: 72/100 (Grade: C)", 0.1),
  ("New best score: 72 (previous best: 0)", 0.1),
  ("Score 72/100 < 80, refining...", 0.2),
  ("🔧 [Step 4/4] Reconciling PRD (iteration 1)...", 0.3),
  ("Invoking Claude (claude-opus-4-7) for PRD reconciliation...", 2.0),
  ("   Claude (claude-opus-4-7) API call completed", 0.1),
  ("Claude responded in 495s — Reconciled PRD (77051 chars)", 0.3),
  ("🔍 [Step 3/4] Reviewing PRD (iteration 2)...", 0.3),
  ("Invoking Codex (gpt-5.3-codex) for PRD review...", 1.5),
  ("   Codex (gpt-5.3-codex) API call completed", 0.1),
  ("Codex responded in 28s", 0.1),
  ("Review score: 58/100 (Grade: D)", 0.1),
  ("Score 58/100 < 80, refining...", 0.2),
  ("🔧 [Step 4/4] Reconciling PRD (iteration 2)...", 0.3),
  ("Invoking Claude (claude-opus-4-7) for PRD reconciliation...", 2.0),
  ("   Claude (claude-opus-4-7) API call completed", 0.1),
  ("Claude responded in 584s — Reconciled PRD (93288 chars)", 0.3),
  ("🔍 [Step 3/4] Reviewing PRD (iteration 3)...", 0.3),
  ("Invoking Codex (gpt-5.3-codex) for PRD review...", 1.5),
  ("   Codex (gpt-5.3-codex) API call completed", 0.1),
  ("Codex responded in 24s", 0.1),
  ("Review score: 67/100 (Grade: C)", 0.1),
  ("Score 67/100 < 80, refining...", 0.2),
  ("🔧 [Step 4/4] Reconciling PRD (iteration 3)...", 0.3),
  ("Invoking Claude (claude-opus-4-7) for PRD reconciliation...", 2.0),
  ("   Claude (claude-opus-4-7) API call completed", 0.1),
  ("Claude responded in 412s — Reconciled PRD (98847 chars)", 0.3),
  ("🔍 [Step 3/4] Reviewing PRD (iteration 4)...", 0.3),
  ("Invoking Codex (gpt-5.3-codex) for PRD review...", 1.5),
  ("   Codex (gpt-5.3-codex) API call completed", 0.1),
  ("Codex responded in 21s", 0.1),
  ("Review score: 85/100 (Grade: B)", 0.1),
  ("New best score: 85 (previous best: 72)", 0.1),
  ("✅ Done (score >= 80). Final score: 85/100", 0.3),
]

__all__ = ["DUMMY_PRD_TEXT", "DUMMY_PRD_STREAM_EVENTS"]
